# analise_de_credito — Dependências de produção
# Instale com: pip install -r requirements.txt

# Manipulação de dados
pandas>=2.0.0
openpyxl>=3.1.0
numpy>=1.26.0

# Exportação Parquet (obrigatório para trilha Athena/S3)
pyarrow>=13.0.0        # EXPORT_FORMAT=parquet em 04_export.py

# Conexão com bancos
pyodbc>=5.0.0          # SQL Server
boto3>=1.34.0          # AWS (S3, Athena)
pyathena>=3.0.0        # Athena query direto via Python

# Utilitários
python-dotenv>=1.0.0   # Leitura do .env
loguru>=0.7.0          # Logging estruturado
